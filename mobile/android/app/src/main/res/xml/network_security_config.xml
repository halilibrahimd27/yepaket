<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ şifresiz HTTP'yi varsayılan olarak engeller. Üretim trafiği
  zaten HTTPS'tir; bu istisna yalnızca yerel geliştirme adreslerini kapsar:
  emülatörün host makineye eriştiği 10.0.2.2 ve localhost.

  Gerçek alan adları burada YOKTUR — production'da şifresiz bağlantı kabul
  edilmez.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
